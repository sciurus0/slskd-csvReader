# Runtime dependencies for slskd-csvReader (DEV/PROD scripts).
# Python 3.10+ recommended.

requests>=2.28,<3
slskd-api>=0.1.5

# Optional web UI (uvicorn webapp.app:app)
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
