# macOS
.DS_Store
/checkpoint.pkl
/logs
/api.txt

# Local planning doc (not tracked)
PROJECT_PLAN.md

# CSV pipeline data (exports, queues, reports — not tracked)
*.csv

# Cross-run merge watermarks (ledger is *.csv)
merge_state.json

# Local throwaway / diagnostic scripts (not maintained)
rebuild_queue_once.py

# Python bytecode (keep repo to source only)
__pycache__/
*.py[cod]
*$py.class
